# Инструментальные правила (Tooling)

Эти правила описывают стандарты работы с инструментами и процессами, которые могут быть перенесены в другие проекты.

## 📝 Документация и ADR
- Любые значимые архитектурные решения фиксируются в виде ADR в папке `docs/adr/`.
- ADR должны иметь порядковый номер и статус (Proposed, Accepted, Rejected, Deprecated).
- Для сложных диаграмм используется подход "Diagrams as Code" (PlantUML).

## 🌿 Git и Коммиты
- Коммиты должны быть атомарными и иметь осмысленные сообщения на русском или английском языке.
- Предпочтительный формат: `type: description` (например, `docs: обновить схему архитектуры`).

## 🤖 Поведение AI-агента
- Перед началом крупной задачи всегда проверяй `activeContext.md`.
- Используй `update_todo_list` для отслеживания прогресса.
- Если задача требует специфических знаний (например, рендеринг диаграмм), загружай соответствующий навык из `.kilocode/skills/`.
- Не дублируй информацию: если данные есть в документации проекта, ссылайся на них, а не копируй.

## 🐞 Отладка и исправление ошибок
- При возникновении ошибки попробуй исправить её самостоятельно на основе контекста.
- Если ошибка повторяется или не ясна, **обязательно** обратись к `context7` для получения актуальной документации.
- Если после исправления на основе документации ошибка сохраняется, проведи глубокий поиск и в `context7`, и в **Интернете** (Tavily/Brave Search).
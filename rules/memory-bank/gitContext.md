# Git Context: Проекты и Репозитории

Этот файл содержит карту всех Git-репозиториев внутри проекта. Используется AI-агентами для правильной навигации и выполнения Git-команд.

## 🗺️ Карта репозиториев

| Путь | Описание | Роль |
| :--- | :--- | :--- |
| `./` | Основной проект (rag4code) | Root Repo |
| `./.kilocode/` | Глобальные правила и скиллы | Nested Repo |
| `./hsm/` | Инструмент Hyper Stack Manager | Nested Repo |
| `./hsm/.kilocode/` | Memory Bank для HSM | Nested Repo |
| `./packages/rag4code-chunker/` | Модуль чанкирования | Nested Repo |
| `./packages/rag4code-ollama/` | Адаптер для Ollama | Nested Repo |
| `./packages/rag4code-qdrant/` | Адаптер для Qdrant | Nested Repo |

## ⚠️ Правила работы

1. **Изоляция**: При внесении изменений в файлы внутри вложенного репозитория, Git-команды (`add`, `commit`, `push`) **ОБЯЗАТЕЛЬНО** должны выполняться внутри соответствующей директории.
2. **Атомарность**: Изменения, затрагивающие несколько репозиториев, должны фиксироваться отдельными коммитами в каждом из них.
3. **Синхронизация**: Перед началом работы в вложенном репозитории рекомендуется выполнить `git pull`.

## 🛠️ Команды для агента

### Поиск всех репозиториев
Если ты сомневаешься в актуальности карты, выполни команду для поиска всех директорий `.git`:
```bash
find . -name ".git" -type d -not -path "*/node_modules/*"
```

### Алгоритм коммита
Перед выполнением коммита:
1. Определи, к какому репозиторию относится файл.
2. Выполни `cd <path_to_repo>`.
3. Выполни Git-операции.

## 🔄 Обновление карты
При добавлении или удалении вложенных (nested) Git-репозиториев **ОБЯЗАТЕЛЬНО** обнови этот файл (`gitContext.md`), чтобы сохранить актуальную навигацию для будущих сессий.
# Active Context: HyperGraph

## Текущая задача
Реорганизация проекта: выделение rag4code в отдельный сервис `services/code-rag` и подготовка фундамента для HyperGraph.

## Состояние
- [x] Спроектирована концепция HyperGraph.
- [x] Создана структура `services/code-rag`.
- [x] Исходный код и документация rag4code перенесены в `services/code-rag`.
- [x] Корневой `hsm.yaml` обновлен для управления `code-rag` как подпроектом.

## Фокус
Реализация компонентов HyperGraph как отдельных MCP-инструментов.

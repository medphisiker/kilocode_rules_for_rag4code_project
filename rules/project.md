# Проектные правила (Project Specific)

Эти правила специфичны для текущего проекта `rag4code`.

## 🏗️ Структура проекта
- `docs/` — Основная документация.
- `docs/diagrams/` — Исходники PlantUML и отрендеренные PNG. Зеркалирует структуру `docs/`.
- `docs/adr/` — Архитектурные решения.
- `.kilocode/` — Конфигурация AI-агента (Rules, Memory, Skills).

## 🛠️ Технологический стек
- **Backend Sandbox**: uvx, npx.
- **Vector DB**: Qdrant, LanceDB.
- **Embedding**: OpenAI, Ollama.
- **Diagrams**: PlantUML + [plantuml-docker-renderer](https://github.com/VLMHyperBenchTeam/plantuml-docker-renderer).

## 📋 Специфические инструкции
- При создании новых диаграмм всегда размещай их в `docs/diagrams/` по пути, соответствующему Markdown-файлу.
- Ссылка в Markdown должна вести на PNG, а под ней — ссылка на исходный `.puml`.
- Всегда используй `!theme plain` для диаграмм PlantUML.
# Проектные правила (Project Specific)

Эти правила специфичны для текущего проекта `rag4code`.

## 🏗️ Структура проекта
- `docs/` — Основная документация.
- `docs/diagrams/` — Исходники PlantUML и отрендеренные PNG.
- `docs/adr/` — Архитектурные решения.
- `.kilocode/` — Конфигурация AI-агента (Rules, Memory, Skills).
- `src/` — Исходный код (Backend Engine, MCP Server, Chunker).

## 🛠️ Технологический стек
- **Backend Core**: Python (uv).
- **Vector DB**: Qdrant (Docker).
- **Embedding**: Ollama (Qwen3-Embedding).
- **Diagrams**: Mermaid (для общих схем), PlantUML (для деталей).

## 📋 Архитектурные принципы
- **Domain-Driven Design (DDD)**: Бизнес-логика (Core) должна быть полностью отделена от интерфейсов (MCP, Web, CLI).
- **LEGO-принцип**: Каждый компонент (DB, Embedder, Chunker) должен быть заменяемым кирпичиком с четким интерфейсом.
- **Thin MCP Layer**: MCP-сервер является лишь тонким адаптером над Backend Engine.

## 📋 Специфические инструкции
- При создании новых диаграмм Mermaid вставляй их прямо в Markdown.
- Сложные диаграммы PlantUML размещай в `docs/diagrams/` по пути, соответствующему Markdown-файлу.
- Всегда используй `!theme plain` для диаграмм PlantUML.
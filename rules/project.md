# Проектные правила (Project Specific)

Эти правила специфичны для текущего проекта `HyperGraph`.

## 🏗️ Структура проекта
- `services/` — Независимые сервисы (MCP Tools).
- `hsm/` — Инструмент оркестрации стека.

## 🛠️ Технологический стек
- **Backend Core**: Python (uv).
- **Orchestration**: HSM.

## 📋 Архитектурные принципы
- **Micro-services (MCP)**: Каждый компонент — это отдельный сервис.
- **LEGO-принцип**: Компоненты должны быть заменяемыми.
- **Unified Knowledge Graph**: Все данные объединяются в единый граф.

## 📜 Специфические инструкции
- Все комментарии в коде, docstrings и имена переменных должны быть на **английском языке**.
- Сообщения коммитов (Git commit messages) должны быть полностью на **английском языке** в формате Commitizen.

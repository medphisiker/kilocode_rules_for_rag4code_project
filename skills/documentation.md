# Навык: Работа с документацией и диаграммами

Этот навык описывает процесс создания и обновления технической документации в проекте с использованием PlantUML и Docker-рендерера.

## 🛠️ Инструменты
- **PlantUML**: Для сложных, детализированных диаграмм (Sequence, Component, Class). Требует рендеринга в PNG/SVG.
- **Mermaid**: Для простых и средних диаграмм (Flowchart, простые Sequence). Рендерится нативно GitHub/GitLab.
- **Docker Renderer**: `ghcr.io/vlmhyperbenchteam/plantuml-renderer` для PlantUML.

## 📋 Алгоритм создания диаграммы (PlantUML)
1.  Создай исходный файл `.puml` в `docs/diagrams/<path_to_md_file>/`.
2.  Используй `!theme plain`, `skinparam linetype ortho` и осмысленные алиасы.
3.  Запусти рендеринг через Docker:
    ```bash
    docker run --rm -v "$(pwd)/docs:/docs" ghcr.io/vlmhyperbenchteam/plantuml-renderer
    ```
4.  Вставь в Markdown-файл ссылку на PNG: `![title](diagrams/<path>/name.png)`.
5.  Добавь под картинкой ссылку на исходник: `[Исходник диаграммы](diagrams/<path>/name.puml)`.

## 📋 Алгоритм создания диаграммы (Mermaid)
1.  Вставляй код диаграммы **прямо в Markdown-файл** документации внутрь блока `mermaid`:
    ````markdown
    ```mermaid
    flowchart TD
        ...
    ```
    ````
2.  **Не создавай** отдельные `.mmd` файлы и не используй Docker-рендерер для Mermaid.
3.  GitHub и GitLab рендерят такие блоки автоматически.

## � Работа с ADR
1.  Создай новый файл в `docs/adr/NNN-name.md`.
2.  Используй стандартную структуру: Статус, Контекст, Решение, Последствия.
3.  Если решение сложное — приложи диаграмму, созданную по алгоритму выше.